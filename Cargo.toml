[workspace]
resolver = "2"

members = [
    "nexus-common",
    "nexus-server",
    "nexus-client",
    "nexus-tracker",
]

[workspace.package]
version = "0.8.4"
edition = "2024"

[profile.release]
strip = false
lto = "thin"
