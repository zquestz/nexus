[workspace]
resolver = "2"

members = [
    "nexus-common",
    "nexus-server",
    "nexus-client",
    "nexus-tracker",
]

[workspace.package]
version = "0.9.1"
edition = "2024"

[profile.release]
strip = false
lto = "thin"
